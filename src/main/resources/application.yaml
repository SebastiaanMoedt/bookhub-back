spring:
  application:
    name: bookhub-back
  datasource:
    url: jdbc:sqlserver://localhost;databasename=BOOKHUB;integratedSecurity=false;encrypt=false;trustServerCertificate=false
    username: sa
    password: Pa$$w0rd

  jpa:
    show-sql: true
    hibernate.ddl-auto: update
    open-in-view: false # pour corriger un warning au lancement de l'app

  spring:
    messages:
      encoding: UTF-8